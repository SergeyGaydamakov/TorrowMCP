# Torrow MCP Service

MCP сервер для сервиса Torrow, который позволяет управлять заметками и каталогами через Model Context Protocol.

> 📖 **Быстрая настройка**: См. [SETUP.md](SETUP.md) для краткой инструкции по настройке подключения.
> 
> 🔧 **Запуск в режиме сервиса**: См. [SERVICE.md](SERVICE.md) для инструкций по запуску как системного сервиса (Windows Service, systemd).

## Возможности

### Управление Заметками (Notes)
- ✅ **Создание заметки** - создает новую заметку в текущем каталоге
- ✅ **Изменение заметки** - изменяет текущую заметку  
- ✅ **Удаление заметки** - удаляет текущую заметку
- ✅ **Получение информации о заметке** - выводит данные заметки
- ✅ **Поиск заметок** - ищет заметки по фразе и тегам

### Управление Каталогами (Archives)
- ✅ **Создание каталога** - создает новый каталог (максимум 10)
- ✅ **Изменение каталога** - изменяет текущий каталог
- ✅ **Удаление каталога** - удаляет текущий каталог с опциональным удалением всех заметок
- ✅ **Получение информации о каталоге** - выводит данные каталога
- ✅ **Список каталогов** - выводит все доступные каталоги

### Контекст общения
- ✅ **Выбор каталога** - устанавливает текущий каталог
- ✅ **Выбор заметки** - устанавливает текущую заметку
- ✅ **Статус контекста** - показывает текущий каталог и заметку

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd torrow-mcp-service
```

2. Установите зависимости:
```bash
npm install
```

3. Скопируйте файл конфигурации:
```bash
cp env.example .env
```

4. Укажите ваш токен Torrow в файле `.env`:
```env
TORROW_TOKEN=your_torrow_token_here
```

5. Соберите проект:
```bash
npm run build
```

## Использование

### Запуск в режиме разработки

**Консольный режим (stdio)** - для MCP клиентов (Cursor, Claude Desktop):
```bash
npm run dev
```

**HTTP режим** - независимый сервис:
```bash
npm run dev:http
```

### Запуск собранной версии

**Консольный режим (stdio)** - для MCP клиентов:
```bash
npm run build
npm start
```

**HTTP режим** - независимый сервис:
```bash
npm run build
npm run start:http
# или с указанием порта
PORT=3000 npm run start:http
```

**Примечание:** 
- Консольный режим используется MCP клиентами (Cursor, Claude Desktop) для запуска сервера как дочернего процесса
- HTTP режим запускает сервер как независимый HTTP сервис на порту 3000 (по умолчанию)

### Запуск тестов
```bash
npm test
```

### Запуск тестов в режиме наблюдения
```bash
npm run test:watch
```

## Конфигурация

### Авторизация и токены

Сервер поддерживает два режима работы с разными способами авторизации:

#### STDIO режим (консольный)

В STDIO режиме токен берется из переменной окружения:

1. **Переменная окружения** (рекомендуется):
```bash
export TORROW_TOKEN="your_token"
```

2. **Файл .env**:
```env
TORROW_TOKEN=your_token
```

3. **В конфигурации MCP клиента** (в секции `env`):
```json
{
  "mcpServers": {
    "torrow": {
      "command": "node",
      "args": ["path/to/index.js"],
      "env": {
        "TORROW_TOKEN": "your_token"
      }
    }
  }
}
```

#### HTTP режим

В HTTP режиме токен передается клиентом в заголовке `Authorization` согласно стандарту MCP:

```
Authorization: Bearer <your_token>
```

- Токен передается клиентом при каждом запросе
- Сервер использует токен клиента для всех вызовов Torrow API
- Токен из переменных окружения сервера **не используется** в HTTP режиме (кроме режима разработки с `DANGEROUSLY_OMIT_AUTH=1`)

**Примечание:** Токен может быть с префиксом "Bearer " или без него - сервер автоматически добавит префикс если его нет.

## Настройка подключения к MCP серверу

После установки и сборки проекта, вам нужно настроить подключение к MCP серверу в вашем клиентском приложении. Ниже приведены инструкции для различных клиентов.

### Для Claude Desktop

1. Откройте файл конфигурации Claude Desktop:
   - **Windows**: `%AppData%\Claude\claude_desktop_config.json`
   - **macOS/Linux**: `~/Library/Application Support/Claude/claude_desktop_config.json`

2. Если файл не существует, создайте его.

3. Добавьте конфигурацию сервера в секцию `mcpServers`:

   **Windows:**
   ```json
   {
     "mcpServers": {
       "torrow": {
         "command": "node",
         "args": ["C:\\Sergeyg\\Torrow\\AI\\TorrowMCP\\dist\\index.js"],
         "env": {
           "TORROW_TOKEN": "your_torrow_token_here"
         }
       }
     }
   }
   ```

   **macOS/Linux:**
   ```json
   {
     "mcpServers": {
       "torrow": {
         "command": "node",
         "args": ["/absolute/path/to/TorrowMCP/dist/index.js"],
         "env": {
           "TORROW_TOKEN": "your_torrow_token_here"
         }
       }
     }
   }
   ```

   **Важно:** 
   - Замените путь на абсолютный путь к вашему проекту
   - Замените `your_torrow_token_here` на ваш реальный токен Torrow
   - На Windows используйте двойные обратные слеши (`\\`) или прямые слеши (`/`) в путях
   - **В STDIO режиме токен берется из переменной окружения `TORROW_TOKEN`**, указанной в секции `env` конфигурации

4. Сохраните файл и перезапустите Claude Desktop.

### Для Cursor

#### Консольный режим (stdio) - рекомендуется

1. Откройте настройки Cursor (File → Preferences → Settings или `Ctrl+,` / `Cmd+,`)

2. Найдите раздел MCP или откройте файл конфигурации MCP напрямую:
   - **Windows**: `%AppData%\Cursor\User\globalStorage\mcp.json` или через настройки
   - **macOS**: `~/Library/Application Support/Cursor/User/globalStorage/mcp.json`
   - **Linux**: `~/.config/Cursor/User/globalStorage/mcp.json`

3. Добавьте конфигурацию сервера для консольного режима:

   ```json
   {
     "mcpServers": {
       "torrow-mcp-service": {
         "command": "node",
         "args": ["C:\\Sergeyg\\Torrow\\AI\\TorrowMCP\\dist\\index.js"],
         "env": {
           "TORROW_API_BASE": "https://torrow.net",
           "TORROW_TOKEN": "your_torrow_token_here",
           "MCP_SERVER_NAME": "torrow-mcp-service"
         }
       }
     }
   }
   ```

   **ВАЖНО:** 
   - Ключ в `mcpServers` (например, `"torrow-mcp-service"`) должен совпадать с именем сервера, которое показывает `list_mcp_resources()`
   - Если Cursor показывает `user-torrow-mcp-service`, измените ключ на `"user-torrow-mcp-service"` или установите переменную окружения `MCP_SERVER_NAME=torrow-mcp-service` в секции `env`
   - Имя сервера используется для идентификации ресурсов, поэтому несоответствие может привести к тому, что GPT не сможет использовать ресурсы
   - **В STDIO режиме токен берется из переменной окружения `TORROW_TOKEN`**, указанной в секции `env` конфигурации

4. Сохраните и перезапустите Cursor.

#### HTTP режим

Для использования HTTP режима необходимо сначала запустить сервер:

1. **Запустите HTTP сервер:**
   ```bash
   npm run build
   npm run start:http
   # или с указанием порта
   PORT=3000 npm run start:http
   ```

2. **Откройте настройки Cursor** (File → Preferences → Settings или `Ctrl+,` / `Cmd+,`)

3. **Найдите раздел MCP** или откройте файл конфигурации MCP напрямую:
   - **Windows**: `%AppData%\Cursor\User\globalStorage\mcp.json`
   - **macOS**: `~/Library/Application Support/Cursor/User/globalStorage/mcp.json`
   - **Linux**: `~/.config/Cursor/User/globalStorage/mcp.json`

4. **Добавьте конфигурацию сервера для HTTP режима:**

   ```json
   {
     "mcpServers": {
       "torrow-mcp-service": {
         "url": "http://localhost:3000/mcp",
         "headers": {
           "Authorization": "Bearer your_torrow_token_here"
         }
       }
     }
   }
   ```
   
   **Примечание:** Некоторые MCP клиенты могут автоматически передавать токен из конфигурации в заголовке `Authorization`. Если ваш клиент поддерживает секцию `headers`, используйте её для указания токена. В противном случае, клиент должен передавать токен в заголовке `Authorization: Bearer <token>` при каждом запросе.

   **Важно:**
   - Используйте `"url"` вместо `"command"` и `"args"` для HTTP режима
   - URL должен указывать на `/mcp` эндпоинт (например: `http://localhost:3000/mcp`)
   - Порт должен соответствовать порту, на котором запущен сервер (по умолчанию 3000)
   - Если сервер запущен на другом хосте, замените `localhost` на соответствующий адрес
   - Убедитесь, что HTTP сервер запущен перед использованием в Cursor
   - **Авторизация:** В HTTP режиме токен передается клиентом в заголовке `Authorization: Bearer <token>` согласно стандарту MCP
   - Клиент должен включать токен в заголовке `Authorization` при каждом запросе
   - Токен клиента используется для всех вызовов Torrow API
   - Если токен отсутствует или невалиден, сервер вернет HTTP 401 Unauthorized

5. **Сохраните и перезапустите Cursor.**

**Примечание:** Консольный режим (stdio) рекомендуется для большинства случаев, так как он не требует предварительного запуска сервера и работает более стабильно с MCP клиентами.

### Альтернативный способ: использование .env файла

Если вы предпочитаете хранить токен в файле `.env` (который уже создан на шаге установки), вы можете не указывать `env` в конфигурации, но убедитесь, что файл `.env` находится в корне проекта:

```json
{
  "mcpServers": {
    "torrow": {
      "command": "node",
      "args": ["C:\\Sergeyg\\Torrow\\AI\\TorrowMCP\\dist\\index.js"]
    }
  }
}
```

Сервер автоматически загрузит переменные из `.env` файла.

### Использование режима разработки

Для разработки можно использовать TypeScript напрямую с `tsx`:

```json
{
  "mcpServers": {
    "torrow": {
      "command": "npx",
      "args": ["tsx", "C:\\Sergeyg\\Torrow\\AI\\TorrowMCP\\src\\index.ts"],
      "env": {
        "TORROW_TOKEN": "your_torrow_token_here"
      }
    }
  }
}
```

### Проверка подключения

После настройки и перезапуска клиента:

1. Убедитесь, что проект собран: `npm run build`
2. Проверьте, что токен указан правильно
3. В клиенте должны появиться доступные инструменты (tools), ресурсы (resources) и промпты (prompts) от сервера Torrow

### Отладка

Если сервер не подключается:

1. Проверьте путь к файлу `dist/index.js` - он должен быть абсолютным
2. Убедитесь, что проект собран (`npm run build`)
3. Проверьте токен в переменных окружения
4. Проверьте логи клиента на наличие ошибок
5. Для отладки можно использовать MCP Inspector:
   ```bash
   npm run inspector
   ```

## Формат фраз

Заметки и каталоги создаются из фраз в специальном формате:

```
<название>.<текст>#тег1#тег2#тег3
```

### Примеры:

- `Рецепты. Описание приготовления блюд. #Еда` 
  - Название: "Рецепты"
  - Текст: "Описание приготовления блюд."
  - Теги: ["Еда"]

- `Рецепт макарон. Сварить макароны 15 минут. #Рецепты#Еда#Макароны`
  - Название: "Рецепт макарон"  
  - Текст: "Сварить макароны 15 минут."
  - Теги: ["Рецепты", "Еда", "Макароны"]

- `Простая заметка` 
  - Название: "Простая заметка"
  - Текст: отсутствует
  - Теги: отсутствуют

## MCP Resources

### `torrow://note/{torrowId}`
Получает информацию о заметке по ID.

### `torrow://archive/{torrowId}`  
Получает информацию о каталоге по ID.

### `torrow://archives`
Получает список всех каталогов.

## MCP Tools

### `create_note`
Создает новую заметку в текущем каталоге.

**Параметры:**
- `phrase` - фраза в формате `<название>.<текст>#тег1#тег2`

### `update_note`
Изменяет текущую заметку.

**Параметры:**
- `phrase` - новая фраза в формате `<название>.<текст>#тег1#тег2`

### `delete_note`
Удаляет текущую заметку.

### `search_notes`
Ищет заметки в текущем каталоге.

**Параметры:**
- `phrase` (опционально) - поисковая фраза
- `tags` (опционально) - массив тегов для фильтрации
- `limit` (опционально) - максимальное количество результатов (по умолчанию: 10)

### `create_archive`
Создает новый каталог.

**Параметры:**
- `phrase` - фраза в формате `<название>.<текст>#тег1#тег2`

### `update_archive`
Изменяет текущий каталог.

**Параметры:**
- `phrase` - новая фраза в формате `<название>.<текст>#тег1#тег2`

### `delete_archive`
Удаляет текущий каталог.

**Параметры:**
- `cascade` (опционально) - удалить все заметки в каталоге (по умолчанию: false)

## MCP Prompts

### `select_archive`
Выбирает каталог по названию и делает его текущим.

**Параметры:**
- `archiveName` - название каталога

### `select_note`
Выбирает заметку по названию или индексу и делает её текущей.

**Параметры:**
- `noteName` (опционально) - название заметки
- `noteIndex` (опционально) - индекс заметки из результатов поиска (начиная с 1)

### `context_status`
Показывает текущий статус контекста (текущий каталог и заметка).

## Ограничения

- Максимальное количество каталогов: 10
- Максимальная длина названия: 100 символов
- При создании каталога/заметки проверяется уникальность названия
- Операции изменения/удаления работают только с текущими объектами

## Архитектура

```
src/
├── common/          # Общие типы и ошибки
├── context/         # Управление контекстом общения
├── prompts/         # MCP prompts
├── resources/       # MCP resources  
├── tools/           # MCP tools
├── torrow/          # Клиент Torrow API
├── util/            # Утилиты
├── tests/           # Тесты
└── index.ts         # Главный файл сервера
```

## Разработка

### Требования
- Node.js 18+
- TypeScript 5+

### Установка зависимостей разработки
```bash
npm install
```

### Линтинг и форматирование
Проект использует ESLint и Prettier (настройки в package.json).

### Тестирование  
Используется Jest с поддержкой TypeScript и ESM модулей.

## Лицензия

MIT
